<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper

	namespace="ltd.newbee.mall.newbeemall.dao.CountSkuCategoryMapper">
	<!--dao层mapper，java文件名 -->

	<select id="countnumberOfCategoryId"
		resultType="java.lang.Integer">
		<!--id是mapper的方法名，resultType是指定的返回类型 -->
		select
		i.goods_name,i.goods_intro,i.goods_cover_img,i.goods_carousel,i.goods_detail_content,
		i.original_price,i.selling_price,s.col_name,s.col,
		c.category_id
		from
		tb_newbee_mall_goods_info as i
		join tb_newbee_mall_goods_category
		as c
		on i.goods_category_id=c.category_id
		join sub_category as s
		on
		s.goods_id=i.goods_id
		<where> c.category_id=#{categoryId} or
		c.category_id in
		(select
		c2.category_id
		from
		tb_newbee_mall_goods_category as c
		join
		tb_newbee_mall_goods_category as
		c2
		on c.category_id=c2.parent_id
		where
		c.category_id=#{categoryId}
		)and
			<foreach item="col" collection="cols" separator=" OR "
				open="(" close=")">
				col=#{col}
			</foreach>
		</where>

	</select>

</mapper>

